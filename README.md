#展示页面
可视化展示所有数据

##打包
```
>npm run deploy
```
生成的文件在dist目录中。
###因为是windows下，需要手动复制静态文件：
将`lib目录复制到dist目录下，将favicon.ico复制到dist目录下。

##调试
```
>npm run dev
```
##域名和接口配置路径
`src/constants/actionsApi.js`

